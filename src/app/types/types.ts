export interface date {
    dateStr: string,
    dateName: string,
    percent?: number,
    absolutePercent?: number,
}

export interface project {
    startDate: string,
    endDate: string,
    projectName: string,
}